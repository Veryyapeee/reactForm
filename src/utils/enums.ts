// Enums
export enum INPUT {
    INPUT = 'input',
}

export enum TEXTAREA {
    TEXTAREA = 'textarea'
}

export enum SELECT {
    SELECT = 'select'
}

export enum TYPE {
    TEXT = 'text',
    PASSWORD = 'password'
}

export enum TYPEDATE {
    DATE = 'date'
}